# Runtime
ENVIRONMENT=development
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8000

# Provider credentials (Hour 2)
ANTHROPIC_API_KEY=
ANTHROPIC_DEFAULT_MODEL=claude-haiku-4-5-20251001
GEMINI_API_KEY=
GEMINI_DEFAULT_MODEL=gemini-2.5-flash

# Postgres (Hour 4) — matches docker-compose.yaml
DATABASE_URL=postgresql+asyncpg://gateway:gateway@localhost:5435/gateway
